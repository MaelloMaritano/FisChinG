Per compilare:
cmake -B build
cmake --build build

Per eseguire Tappa *N*:
build/Tappa*N*

Per giocare:
premere un tasto qualsiasi (mouse o tastiera) per andare avanti
premere le frecce direzionali o WASD nel minigioco